/**
 * ThemeStyleInjector
 *
 * Previously injected CSS custom properties from the active theme.
 * The theme system has been removed; styles now come from CSS variables
 * and Tailwind configuration. This component is retained as a no-op
 * so existing layout code that renders it does not break.
 */

export function ThemeStyleInjector() {
  return null;
}
