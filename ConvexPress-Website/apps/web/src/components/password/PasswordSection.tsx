/**
 * Password Section Component
 *
 * A self-contained password management section for use in dashboard pages.
 * Wraps PasswordLastChanged and provides the change/add password action.
 *
 * This is a convenience wrapper around PasswordChangeSection for use
 * outside the AccountSettingsForm context. The primary integration point
 * is PasswordChangeSection within AccountSettingsForm.
 *
 * Re-exports PasswordLastChanged for convenience.
 */

export { PasswordLastChanged } from "./PasswordLastChanged";
