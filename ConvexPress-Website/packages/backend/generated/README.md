# CONSUMER APP - These are NOT generated Convex files

These files are **hand-written consumer stubs** using `anyApi` from `convex/server`.
They are NOT the output of `npx convex dev` or `npx convex codegen`.

DO NOT run any Convex generation commands in this app.
DO NOT replace these files with real Convex codegen output.

The ConvexPress-Admin owns the Convex deployment. This app is a consumer only.
