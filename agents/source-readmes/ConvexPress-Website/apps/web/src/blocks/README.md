# ConvexPress Website Official Blocks

Website block renderers that are not part of the core registry are discovered
from `apps/web/src/blocks/<block-id>/manifest.tsx`.

Use `apps/web/src/blocks.local/` for site-specific blocks that must survive
platform updates.
